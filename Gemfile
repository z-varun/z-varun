source "https://rubygems.org"

# Jekyll is the core static site generator
gem "jekyll"

# Use the official GitHub Pages gem to ensure local environment
# matches the remote one. This includes the theme and plugins.
gem "github-pages", group: :jekyll_plugins
gem 'jekyll-feed'
gem 'faraday-retry'
